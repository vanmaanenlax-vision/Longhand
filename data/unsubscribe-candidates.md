# Unsubscribe candidates (EXAMPLE TEMPLATE)

Propose-only list. Never click unsubscribe without the operator's explicit word in chat.

Ship empty or with fake rows only. Never copy a live candidate list into a public template.

## Format
- `sender@domain` — short note (why it's a candidate)

## Example (fake — replace or delete)
- `promo@example.com` — retailer
