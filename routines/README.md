# Shipping routines (scrubbed job text)

Eight routines for the public template. Folder slug = install id.

| folder | notes |
|---|---|
| `gmail-weekday-watch` | inbox triage + watch-feed bridge |
| `x-sweep` | weekday X sweep |
| `x-sweep-weekend` | weekend X sweep |
| `intelligence-scout` | Monday intelligence scout |
| `memory-commit` | nightly memory commit |
| `memory-audit` | Sunday memory audit |
| `quiet-flush` | morning quiet-hours flush |
| `channel-listener` | inbound channel listener (often ships paused) |

Job text only under each folder (`job.md`). Live automations stay separate.
